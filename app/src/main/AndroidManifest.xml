<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.rofleksey.animewatcher">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/zero2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/zero2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="ru.rofleksey.animewatcher.TitleListActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ru.rofleksey.animewatcher.EpisodeListActivity" />
        <activity android:name="ru.rofleksey.animewatcher.SearchActivity" />
    </application>

</manifest>